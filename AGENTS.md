# Agent Instructions

Run repository development tools through `dev.sh` so they use the project's
configured environment:

```bash
./dev.sh -c '<command>'
```

The first invocation may take several minutes while dependencies are installed.
Later invocations should be faster.
